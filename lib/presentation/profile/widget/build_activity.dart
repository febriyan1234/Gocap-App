import 'package:flutter/material.dart';

class Buildactivitiy extends StatelessWidget{
  const Buildactivitiy({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Ini halaman Aktivitas",
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.w600),
      ),
    );
  }

}